#include <bits/stdc++.h>
using namespace std;

int main()
{
    int *ptr = new int(6);
    cout << ptr << endl;
    cout << &ptr << endl;
    cout << *ptr << endl;

    // check and see the working and 3 steps followed by thr new operator

    return 0;
}
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a;
    char c;
    cout << "Enter the number: " << endl;
    cin >> a;

    // inputting
    cout << "Enter the character: " << endl;
    cin >> c;
    cout << a << " " << c << endl;

    // command for compilation
    // and then it is runned

    // cmd for interview = g++ -std=gnu++20 main.cpp -o main

    // check use of cin, cout, >>, <<
    // check if any other type of input is given what will happen suppose if asc 12 is given then why output comes as 0%.
    // also check use of forward slash, backward slash, \n, \t, /, \, and so on
    return 0;
}